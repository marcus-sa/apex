import { Component } from '@angular/core';

import { AuthDialogComponent } from '@apex/client';

@Component({
  selector: 'apex-supabase-auth-dialog',
  standalone: true,
  template: ``,
})
export class SupabaseAuthDialogComponent implements AuthDialogComponent {}
